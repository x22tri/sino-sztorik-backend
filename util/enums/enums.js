const SimilarType = Object.freeze({
  APPEARANCE: 'appearance',
  MEANING: 'meaning',
});

const InteractiveWordType = Object.freeze({
  PRIMITIVE: 'p',
  KEYWORD: 'k',
});

module.exports = {
  SimilarType,
  InteractiveWordType,
};
